{
  "buildCommand": "pnpm --filter @workspace/soukma run build",
  "outputDirectory": "artifacts/soukma/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
