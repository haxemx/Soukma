{
  "buildCommand": "cd artifacts/soukma && pnpm run build",
  "outputDirectory": "artifacts/soukma/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
